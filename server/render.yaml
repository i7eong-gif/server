services:
  - type: web
    name: postock-server
    runtime: docker
    dockerfilePath: ./Dockerfile
    envVars:
      - key: ENV
        value: prod
      - key: ADMIN_TOKEN
        generateValue: true
    disk:
      name: sqlite-data
      mountPath: /app/data
      sizeGB: 1
